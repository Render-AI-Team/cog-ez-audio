build:
  gpu: true
  python_version: "3.10"
  system_packages:
    - ffmpeg
    - libsndfile1
    - libavcodec-dev
    - libavformat-dev
    - libavutil-dev
    - libavdevice-dev
    - libavfilter-dev
    - libswscale-dev
    - libswresample-dev
    - pkg-config
  python_requirements: requirements.txt

predict: predict.py:Predictor

train: src/TrainerCog.py:Trainer
